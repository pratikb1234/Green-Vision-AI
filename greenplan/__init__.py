"""GreenGrid: an AI decision engine for urban tree-planting priority.

Public entry point:

    from greenplan import engine, config
    cfg = config.load_config("config/city.yaml")
    engine.run(cfg)
"""

__version__ = "0.1.0"
