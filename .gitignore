.venv/
__pycache__/
*.pyc
# trained memory + metadata are runtime state, regenerated on each run
models/
outputs/
# Windows shortcut junk
*.lnk
# local agent/dev config — may contain secrets and machine-specific paths
.claude/
